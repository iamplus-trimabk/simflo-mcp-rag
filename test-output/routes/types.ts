export interface TaskDetailScreenRouteParams { animation_duration: number, easing: string, show_success_message: number, confirm_discard: number; }

export interface LoginScreenRouteParams { animation_duration: number, easing: string; }

export interface HomeScreenRouteParams { animation_duration: number, easing: string, task_id: string; }